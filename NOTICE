# NOTICE

Open EHR Mobile 是一个纯前端移动端 EHR 演示项目。

本项目内的员工、组织、联系方式、流程编号、附件名称、薪资、考勤、审批、申请单据等内容均为虚构 mock 数据，仅用于本地演示和二次开发参考。

项目不包含真实企业数据，不连接真实后端服务，不提供真实移动办公平台能力，也不包含任何生产环境配置。

第三方依赖以 `package.json` 和 `package-lock.json` 为准，使用时请同时遵守对应依赖的开源许可。
